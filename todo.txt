Title widget:
- Draw new logo that fits updated gui

Stock widget:
- add parameter to avoid unit conversion etc. for string fields
- fix featureManager for invalid images (no features)

Grid view widget:
- add button cooldown for deletion/addition

General:
- Add setup.py file
- add config.ini file 

REFACTORING:
- figure out font import issue
- store plate/router data using custom objects
- make image widgets work
- better manage preview file deletion

file management config:
- permanent data does not need to be modified at runtime (arrays passed automatically)
- should optimize temp data 
- singleton ok but requires importing mgr into each module (inefficient)