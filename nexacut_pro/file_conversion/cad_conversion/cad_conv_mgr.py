import os
import sys
from stl import mesh

import stl_proc

class CADConversionManager:

    # method does not interact with interface
    def __init__(self, filepath):
        self.filepath = filepath
        self.mesh 
        self.flat_axis 
        # add utils with general file opening/deletion methods

    